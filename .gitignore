# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Firebase Service Account (NEVER commit this!)
serviceAccountKey.json

# Firebase local deploy caches
.firebase/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ignore Gradle project-specific cache directory
.gradle

# Ignore Gradle build output directory
build
.credentials.md
*.keystore

# Large POS reference archive — keep local only (GitHub 100MB limit)
the eco sys/ecosystem-plan/posfinal-main.zip

# OAuth / verification values belong in .credentials.md (gitignored) — never commit here

# Local env — never commit secrets (use .env.local.example as template)
.env
.env.local
.env.production
.env.staging
.env.*.local
!**/.env.local.example

# Finance app local env
the eco sys/finance/beirut-finance-flow-main/.env
the eco sys/finance/beirut-finance-flow-main/.env.production
the eco sys/publish-insight-main/.env

# TWA / mobile release binaries — build artifacts, not source
the eco sys/finance/twa/release/
grabio-mobile/release/
*.apk
*.aab
*.apk.idsig