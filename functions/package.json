{
  "name": "market-flow-functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.23",
    "@types/node": "^20.5.9",
    "ts-node": "^10.9.1",
    "typescript": "^5.5.6"
  }
}
