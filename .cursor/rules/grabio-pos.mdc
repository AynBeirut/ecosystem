---
description: Grabio POS Windows — paths, API, deploy safety.
globs: ["**/eco sys pos/**", "**/the eco sys/**", "**/posfinal-main/**", "**/posSync.ts"]
alwaysApply: false
---

# Grabio POS

- Work only inside `eco sys pos/` — do not edit `../posfinal/` or parent `githup/` repos
- Production reference (read-only): `../posfinal/` — full repo, not pos-v1-only
- Handoff pack: `the eco sys/ecosystem-plan/pos-windows-builder-pack/README.md`
- POS code (edit): `the eco sys/ecosystem-plan/posfinal-main/`
- Roadmap: `PROJECT-PLAN.md`
- API: `https://us-central1-market-flow-7b074.cloudfunctions.net/api`
- Live: `/pos/pairing-code`, `/pos/pair`, `/pos/heartbeat`
- Planned: `/pos/catalog`, `/pos/orders`
- Offline-first; add `js/grabio/` — do not delete `sync-manager.js` yet
- Git remote: `https://github.com/AynBeirut/ecosystem.git` (not posfinal)
- No Firebase prod deploy without Anwar approval
