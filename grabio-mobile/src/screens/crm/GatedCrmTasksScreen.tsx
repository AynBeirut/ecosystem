import React from 'react';
import CrmTasksScreen from './CrmTasksScreen';

/** Team to-do list — not gated on Sales CRM (visits/routes stay in CRM). */
export default function GatedCrmTasksScreen() {
  return <CrmTasksScreen />;
}
